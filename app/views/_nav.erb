<nav class="navbar navbar-toggleable-md navbar-light bg-faded">
  <a class="navbar-brand" href="/">The Tube</a>
  <div class="collapse navbar-collapse">
    <ul class="navbar-nav mr-auto">
      <% if logged_in? && current_user %>
      <li class="nav-item"><%= current_user.name  %></li>
     <form class="form-inline my-2 my-lg-0 align-middle" action="/sessions" method="post">
        <input class="form-control mr-sm-2" type="hidden" name="_method" placeholder="logout" value="delete">
        <button class="btn btn-sm btn-outline-danger my-2 my-sm-0" name="logout" type="submit" value="logout">Logout</button>
      </form>
      <% else %>
        <li class="nav-item">
          <a class="nav-link" href="/users/new">Register</a>
        </li>
        <li class="nav-item">
          <a class="login-link nav-link" href="/sessions/new">Login</a>
        </li>
      <% end %>
    </ul>
  </div>
</nav>

